using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*
		PlayerPrefsManager.UnlockLevel (10);
		print (PlayerPrefsManager.UnlockedLevel (10));
		PlayerPrefsManager.UnlockLevel (4);
		print (PlayerPrefsManager.UnlockedLevel (4));

		PlayerPrefsManager.SetDifficulty (0.8f);
		print (PlayerPrefsManager.GetDifficulty ());
		*/
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
